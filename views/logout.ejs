<!DOCTYPE html>
<html lang="en">
    <head>
        <!-- Scripts you wanna load in the beginning, meta information, & stylesheets etc -->
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/style/stylesheet.css">
        <title>Logout</title>
    </head>
    <body>
        <!-- Everything else. New headers, paragraphs, divs, images, etc. Also scripts for functionality -->
        <div class="reg">
            <form class="regForm" id="regForm" onsubmit="logOut();">
                <label id="logoutLabel">LogOut</label>
                <div class="buttons">
                    <button type="submit" class="myBtn">Logout</button>
                </div>
            </form>
        </div>
        <script src="/js/formScript.js"></script>
    <body>
</html>